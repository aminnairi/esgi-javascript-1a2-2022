// Entier (number)
const age = 28;

// Décimale (number)
const pi = 3.14;

// Afficher le type de la variable
console.log(typeof age);

// Notation exponentielle
const price = 10e8;

// Infinity, +Infinity, -Infinity
let maximumPower = Infinity;

// Nombre entier maximum
const maximum = Number.MAX_SAFE_INTEGER;

// Not a number (pas un nombre, mais de type number)
const failedResult = NaN;
