// On peut écrire une chaîne de caractères en la délimitant avec des apostrophes
const text = 'Hello, world!';

// On peut écrire une chaîne de caractères en la délimitant avec des guillemets
const paragraph = "Hello, world!";

// Récupérer la taille d'une chaîne de caractères
// console.log(paragraph.length);

const firstName = "Amin";
const lastName = "NAIRI";
const fullName = firstName + " " + lastName;

// console.log(fullName);
// "Amin NAIRI"

// ALT GR + 7 (2 fois) = backticks
const fullname = `${firstName} ${lastName}`;
